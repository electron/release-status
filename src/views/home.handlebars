<div class="releases">
  {{#if currentlyReleasing.length}}
    <h4>Currently Releasing</h4>
    <div class="active-releases">
    {{#each currentlyReleasing}}
      {{> releaseSquare this}}
    {{/each}}
    </div>
  {{/if}}
  <h4>Nightly</h4>
  {{> releaseBar lastNightly}}
  <h4>Pre Release</h4>
  {{> releaseBar lastPreRelease}}
  <h4>Supported</h4>
  {{#each latestSupported}}
    {{> releaseBar this}}
  {{/each}}
</div>